"use strict";

// Longitude & Latitude of Helsinki
let helsinki_coordinates = [60.192059, 24.945831];

// Map object initalization with zoom location set to Helsinki
let map = new Map(helsinki_coordinates);

// Map creation
map.createMap("map");
